$('#my-images').append('<%= j render partial: "image", collection: @images %>');
<% if @images.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @images %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>